#include <stdio.h>
/**
 * main - Prints single digits of based 10 from 0
 *
 * Return: 0 Always (Success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	putchar(a);
	putchar('\n');
	return (0);
}
