#include "main.h"
/**
 * swaps_int - function swap the values of two integers
 * @a: the first parameter
 * @b: the second parameter
 *
 */
void swap_int(int *a, int *b);
{
int d;
d = *a;
*a = *b;
*b = d;
}
