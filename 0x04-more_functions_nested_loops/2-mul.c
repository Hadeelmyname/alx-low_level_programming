#include "main.h"
/**
 * mul - Multiplies two integers
 * @a: the first number to multiply
 * @b: the second number to mulitply
 * Return: the result of multiply a and b
 */
int mul(int a, int b)
{

	return (a * b);
}
